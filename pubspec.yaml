name: payment_gateway_testing
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.9.0
  flutter_stripe: ^11.5.0
  flutter_bloc: ^9.1.1
  dartz: ^0.10.1
  flutter_paypal_payment: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
